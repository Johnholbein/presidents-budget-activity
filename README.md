# The President's Budget Activity

Static student-facing build of the LPPP 6350 presidency budget simulation.

Open `index.html` to run the activity. ElevenLabs voiceover audio is generated through a Cloudflare Worker proxy, so the ElevenLabs API key is not stored in this repository.
